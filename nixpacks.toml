# Nixpacks configuration for TypeScript Node.js app

[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]
nixLibs = ["openssl"]

[phases.install]
cmds = ["npm ci", "npx prisma generate"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx prisma migrate deploy && node dist/server.js"
